import React from 'react';
import Layout from '../layout/Index';
import Stays from '../components/Trains';

const HotelsPage = () => {
  return (
    <Layout>
      <Stays/>
    </Layout>
  );
};

export default HotelsPage;
